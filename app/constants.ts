export const API_BASE_URL = process.env.API_BASE_URL ?? '';
export const NEXT_PUBLIC_BASE_URL = process.env.NEXT_PUBLIC_BASE_URL ?? '';

export const SESSION_DURATION = Number(process.env.SESSION_DURATION) ?? 0;
export const ACCESS_DURATION = Number(process.env.ACCESS_DURATION) ?? 0;